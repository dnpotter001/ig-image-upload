info:
  name: Auth
  type: http
  seq: 2

http:
  method: GET
  url: https://accounts.google.com/o/oauth2/auth
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
