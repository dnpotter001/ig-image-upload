info:
  name: Token
  type: http
  seq: 3

http:
  method: GET
  url: https://oauth2.googleapis.com/token
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
